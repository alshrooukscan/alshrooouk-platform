import { theme } from "../../../lib/theme";

export default function DoctorsPage() {
  return (
    <div>
      <h1 style={{ color: theme.navy }}>Doctors</h1>
      <p style={{ color: theme.gray }}>Doctor directory and registration land in Sprint 1.3.</p>
    </div>
  );
}
