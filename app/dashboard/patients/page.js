import { theme } from "../../../lib/theme";

export default function PatientsPage() {
  return (
    <div>
      <h1 style={{ color: theme.navy }}>Patients</h1>
      <p style={{ color: theme.gray }}>Search, registration, and visit records land in Sprint 1.2.</p>
    </div>
  );
}
