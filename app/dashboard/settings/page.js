import { theme } from "../../../lib/theme";

export default function SettingsPage() {
  return (
    <div>
      <h1 style={{ color: theme.navy }}>Settings</h1>
      <p style={{ color: theme.gray }}>Branch management and admin rules land alongside their modules.</p>
    </div>
  );
}
