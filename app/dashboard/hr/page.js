import { theme } from "../../../lib/theme";

export default function HRPage() {
  return (
    <div>
      <h1 style={{ color: theme.navy }}>HR</h1>
      <p style={{ color: theme.gray }}>Employee records and payroll arrive in Phase 4.</p>
    </div>
  );
}
